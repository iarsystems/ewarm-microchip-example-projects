// isymexport steerfile
//
// We need to export the stack symbol and the program entry symbol
// in order to make a valid application image for Atmel BLE OS.

show PROC_STACK$$Base
show PROC_STACK$$Limit
show PROC_STACK$$Length

show __iar_program_start